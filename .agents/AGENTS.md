# Rules

- **Testing Before Pushing**: Do not run `git push` or push changes to the remote GitHub repository automatically. Always commit changes locally and wait for the user to explicitly test and approve pushing.
